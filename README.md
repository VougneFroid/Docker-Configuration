1. Go to your app site where manage.py exist

2. enable your venv, then enter in terminal ``pip freeze > requirements.txt``

3. put the Dockerfiles, .dockerignore, and the docker-compose.yml on the same folder

4. 